<!-- Navigation -->
<nav class="bg-white shadow">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <a href="#" class="text-xl font-bold text-blue-600">U-Innova CRM</a>
      </div>
      <div class="flex space-x-4">
        <%= link_to "Home", root_path, class: "px-3 py-2 text-gray-700 hover:text-blue-600" %>
        <a href="#" class="px-3 py-2 text-gray-700 hover:text-blue-600">About</a>
        <a href="#" class="px-3 py-2 text-gray-700 hover:text-blue-600">Contact</a>
        <% if user_signed_in? %>
          <%= link_to "Exit", destroy_user_session_path, data: { turbo_method: :delete }, class: "px-3 py-2 text-gray-700 hover:text-blue-600" %>
        <% else %>
          <%= link_to "Log in", new_user_session_path, class: "px-3 py-2 text-gray-700 hover:text-blue-600" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>